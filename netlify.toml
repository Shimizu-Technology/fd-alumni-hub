# React/Vite staging Netlify config.
# This branch intentionally differs from main because main's netlify.toml still
# deploys the legacy Next.js production fallback in apps/web.
[build]
  base = "web"
  command = "npm run build"
  publish = "dist"
