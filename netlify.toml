[build]
  base = "."
  command = "npm ci --include=optional && npm --workspace @fd/web install lightningcss-linux-x64-gnu --no-save && npm --workspace @fd/web run build"
  publish = "apps/web/.next"

[[plugins]]
  package = "@netlify/plugin-nextjs"
