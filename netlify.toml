# React/Vite frontend deployment for the Rails-backed FD Alumni Basketball Hub.
# The legacy Next.js app has been archived under archive/legacy-next-app.
[build]
  base = "."
  command = "npm ci --include=optional && npm run build"
  publish = "web/dist"
